{
  "honest-test/src/honest_test/numeric.py:1000000->1000001@DEFAULT_LIMIT = 1_000_000": "The Fibonacci sequence has a huge gap near 1_000_000 (832040 then 1346269), so any limit in (832040, 1346269] yields the identical sequence. +1 does not cross a Fibonacci value, so fibonacci_sequence and numeric_values are unchanged. Verified fib(1000000)==fib(1000001).",
  "honest-test/src/honest_test/numeric.py:1000000->999999@DEFAULT_LIMIT = 1_000_000": "As above: -1 stays within the (832040, 1346269] gap, so the generated sequence is identical. Verified fib(1000000)==fib(999999).",
  "honest-test/src/honest_test/coverage_data.py:2->3@write(path, json.dumps(coverage, indent=2))": "indent= controls only JSON whitespace; the serialized document parses to the identical structure, and honest-check reads coverage.json back via json.loads, so the indentation is not part of the cross-tool contract. Verified the parsed object is identical for indent 1/2/3.",
  "honest-test/src/honest_test/coverage_data.py:2->1@write(path, json.dumps(coverage, indent=2))": "As above: indent= is cosmetic whitespace only; the parsed document is identical and honest-check reads it via json.loads.",
  "honest-test/src/honest_test/determinism.py:1->2@module_name, attr = path.rsplit(\".\", 1)": "Every watch-list path is module.attr with exactly one dot, so rsplit('.', 1) and rsplit('.', 2) return the same two parts. No watch-list entry has two dots.",
  "honest-test/src/honest_test/determinism.py:remove@return None": "Removing the trailing `return None` in nondeterminism_finding lets the function fall off its end, which returns None implicitly — identical to the explicit return.",
  "honest-test/src/honest_test/honesty.py:string->empty@return link_meta(link).get(\"name\") or getattr(link, \"__name__\", \"<link>\")@_name": "_name reads link_meta(link).get('name'); @link always sets name == the function __name__, and a non-link callable has no 'name' key (link_meta returns {}). So get('name') is either the __name__ or None, and `or getattr(__name__)` yields the same string whether the key is 'name' or ''. No constructible link makes name differ from __name__.",
  "honest-test/src/honest_test/authhonesty.py:string->empty@_FAULT_HTTP = {\"forbidden\": 403, \"unauthenticated\": 401, \"client\": 400, \"server\": 500}@<module>#3": "Emptying the 'server' key makes map_fault_to_http('server') miss the dict and hit the .get default, which is also 500 — the same result. The server category maps to 500 either way.",
  "honest-test/src/honest_test/honesty.py:remove@return None@verify_purity": "Removing verify_purity's final `return None` (reached only when the two runs are equal) lets the function fall off its end and return None implicitly — identical.",
  "honest-test/src/honest_test/honesty.py:remove@return None@detect_mutation": "Removing detect_mutation's final `return None` (reached only when the manifest was not mutated) lets the function fall off its end and return None implicitly — identical.",
  "honest-test/src/honest_test/statemachine.py:remove@return None": "Removing the trailing `return None` in _first (reached only when names is empty) lets the function fall off its end and return None implicitly — identical.",
  "honest-test/src/honest_test/mutation.py:0->-1@inner = statement.children[0]": "An expression_statement has exactly one child (the inner expression/assignment); trailing comments are not its children. children[0] == children[-1] always. Verified identical across a 20-source battery.",
  "honest-test/src/honest_test/mutation.py:remove@if statement.type != \"expression_statement\":@_is_annotation_only": "_is_annotation_only's fast-path guard: a non-expression statement's children[0] is never an 'assignment' node, so the assignment check below returns False for it regardless. The guard is an optimisation, not a correctness gate. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:sole-pass@return False@_is_annotation_only": "Replacing _is_annotation_only's guard `return False` with pass lets it fall through to the assignment check, which returns False for any non-annotation statement anyway — same result. Verified across the battery.",
  "honest-test/src/honest_test/mutation.py:sole-pass@continue@_comparison_swaps": "A `if node.type != X: continue` type guard turned into pass: iterating the wrong node type then hits an inner guard (text not in the swap table, or no matching child), which yields no mutant — so no extra mutant is produced. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:sole-pass@continue@_dict_key_swaps": "As above: a node-type guard continue->pass is absorbed by the loop body's inner guard, producing no mutant for the wrong node type. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:sole-pass@continue@_dict_key_swaps#1": "As above: a node-type guard continue->pass is absorbed by the loop body's inner guard, producing no mutant for the wrong node type. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:remove@return None": "Removing _condition_node's trailing `return None` (reached for a non-conditional node) lets it fall off its end and return None implicitly — identical.",
  "honest-test/src/honest_test/mutation.py:remove@if len(keys) < 2:": "Removing the <2-keys guard in _dict_key_swaps is harmless: the cyclic sibling of a sole key is the key itself, and the equal-text check skips that swap, so a dict with fewer than two keys produces no mutant with or without the guard. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:2->1@if len(keys) < 2:": "`len(keys) < 2` -> `< 1` only changes empty-dict handling: a 1-key dict (the only newly-admitted case) still produces no mutant because its cyclic sibling is itself and the equal-text check skips it. Verified identical across the battery. (`< 3` would skip 2-key dicts and is caught.)",
  "honest-test/src/honest_test/mutation.py:remove@if node.type != \"comparison_operator\":@_comparison_swaps": "Removing the node-type guard lets the loop body run for every node; a non-comparison node's children are not comparison-operator tokens, so the inner `text not in _COMPARISON_SWAP` guard yields no mutant. Verified identical across the battery.",
  "honest-test/src/honest_test/mutation.py:remove@if node.type != \"dictionary\":": "Removing the node-type guard lets the loop body run for every node; a non-dictionary node has no child_by_field_name('key'), so keys is empty and the len<2 guard skips it. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:<=-><@if n <= 0:": "_string_of_length is only ever called with n >= 1 (valid uses range(min_len>=1, ...), invalid uses max_len+1>=1 and min_len-1 only when min_len>1; len==0 clamps min to 1 so n=0 never reaches it). The guard is defensive; <= 0 vs < 0 cannot be observed. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:0->-1@if n <= 0:": "As above: the n <= 0 guard is unreachable (n is always >= 1), so n <= 0 vs n <= -1 cannot be observed. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:remove@if n <= 0:": "The n <= 0 guard is unreachable (n is always >= 1); removing it changes nothing, and even at n=0 the fall-through slice (chars*1)[:0] yields the same empty string. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:sole-pass@return \"\"": "As above: the unreachable guard's `return \"\"` -> pass falls through to a slice that also yields \"\" for n=0; n is never negative. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:2->1@for i in range(0, len(items) - 2, 2):@_scan_comparison": "Stepping the triple scan by 1 instead of 2 visits the odd (op, operand, op) triples too, but each fails the operand-op-operand guard and contributes no bound. Verified identical across the battery (including chained comparisons).",
  "honest-test/src/honest_test/length.py:and->or@if kind_left == \"operand\" and kind_op == \"op\" and kind_right == \"operand\":@_scan_comparison#1": "With the step of 2, every visited triple is operand-op-operand, so all three conjuncts are true together and `and` vs `or` give the same result. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:remove@return": "Removing the early `return` after the len-on-left branch in _bound_from_pair lets control fall to the len-on-right branch, which is a no-op whenever the left was len (the right side is then the integer, not a len call). Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:remove@continue": "Comparison operator tokens are unnamed (is_named is False), so after appending the op item the `if child.is_named` operand branch is skipped anyway; the continue is redundant. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:sole-pass@return False": "_is_len_call's `if node.type != \"call\": return False` -> pass falls through to child_by_field_name('function'), which is None for a non-call node, so the function still returns False. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:and->or@if kind_left == \"operand\" and kind_op == \"op\" and kind_right == \"operand\":@_scan_comparison": "With the step of 2, every visited triple is operand-op-operand, so all three conjuncts are true together and the first `and` vs `or` give the same result. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:2->1@for i in range(0, len(items) - 2, 2):@_scan_comparison#1": "Shifting either 2 in `range(0, len(items) - 2, 2)` by -1 (the -2 bound or the step) does not change which indices are visited for well-formed comparisons: the odd (op, operand, op) triples it would add all fail the operand-op-operand guard. Verified identical across the battery.",
  "honest-test/src/honest_test/length.py:1->2@repeats = (n // len(chars)) + 1": "repeats only needs to be enough to cover n before the [:n] slice; (n // len(chars)) + 1 already over-provides, so + 2 (one extra full copy of chars) is sliced away and the result string is identical. Verified across the battery.",
  "honest-test/src/honest_test/length.py:remove@if node.type != \"call\":": "Removing the _is_len_call guard lets a non-call node fall through to child_by_field_name('function'), which is None for a non-call, so `None is not None` makes the function return False anyway. Verified identical across the battery.",
  "honest-test/src/honest_test/honesty.py:remove@return None@verify_idempotency": "Removing verify_idempotency's final `return None` (reached only when the two runs are equal) lets the function fall off its end and return None implicitly — identical. Verified.",
  "honest-test/src/honest_test/predicate.py:remove@return": "Removing the early `return` in _callee_identifier after a builtin fact is set lets control fall to the named-calls branch, adding the builtin name to named_calls. But a builtin always sets a fact (numeric_call/len_call/catchall) whose precedence class is matched before the named_calls branch is ever reached, so the leaked name never changes the classification. Verified identical across the battery.",
  "honest-test/src/honest_test/value_oracle.py:0->-1@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->1@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->-1@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->1@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->1@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->1@\"source_line\": 0,": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->-1@\"source_line\": 0,": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->-1@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:0->-1@return {\"id\": case.get(\"id\", \"\"), \"proved\": report[\"status\"] == \"ok\", \"fault\": faults[0] if faults else None}": "faults[-1] vs faults[0]: the value scenario stops at the first non-ok step, so faults has at most one element and [0]==[-1]. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@return {**context, \"case\": case}": "The step's returned context is unused downstream: when() invokes the case via the closure (not context['case']), and then() is the last step, so a falsey/changed return leaves the oracle result unchanged. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@registry = register_step(registry, \"given\", \"the input is supplied\", given)@_bound_registry": "The emptied string is the step KIND argument to register_step; match_step matches purely on pattern text and ignores the registered kind, so the value scenario still resolves. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@registry = register_step(registry, \"when\", \"the function is called\", when)@_bound_registry": "The emptied string is the step KIND argument to register_step; match_step matches purely on pattern text and ignores the registered kind, so the value scenario still resolves. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@registry = register_step(registry, \"then\", \"the value oracle holds\", then)@_bound_registry": "The emptied string is the step KIND argument to register_step; match_step matches purely on pattern text and ignores the registered kind, so the value scenario still resolves. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@\"name\": \"value oracle\",@<module>#1": "The scenario name is not read by run_value_case (it returns id/proved/fault, never the report name). Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#2": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#6": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#2": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#6": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#2": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@\"tags\": [],": "The scenario tags are unused by run_value_case; emptying or swapping them cannot change a value case's result. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#6": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@\"source_line\": 0,": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@return {\"id\": case.get(\"id\", \"\"), \"proved\": report[\"status\"] == \"ok\", \"fault\": faults[0] if faults else None}@run_value_case#1": "faults[-1] vs faults[0]: the value scenario stops at the first non-ok step, so faults has at most one element and [0]==[-1]. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:string->empty@return {\"id\": case.get(\"id\", \"\"), \"proved\": report[\"status\"] == \"ok\", \"fault\": faults[0] if faults else None}@run_value_case": "faults[-1] vs faults[0]: the value scenario stops at the first non-ok step, so faults has at most one element and [0]==[-1]. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@\"source_line\": 0,": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"given\", \"resolved_kind\": \"given\", \"text\": \"the input is supplied\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@\"tags\": [],": "The scenario tags are unused by run_value_case; emptying or swapping them cannot change a value case's result. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"when\", \"resolved_kind\": \"when\", \"text\": \"the function is called\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#1": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@{\"kind\": \"then\", \"resolved_kind\": \"then\", \"text\": \"the value oracle holds\", \"source_line\": 0},@<module>#3": "The step source_line is carried for the gherkin Step shape but never read by the oracle (run_scenario matches on text); shifting or emptying it cannot change a value case's pass/fail. Verified identical across an 11-case battery.",
  "honest-test/src/honest_test/value_oracle.py:key->sibling@return {\"id\": case.get(\"id\", \"\"), \"proved\": report[\"status\"] == \"ok\", \"fault\": faults[0] if faults else None}@run_value_case": "faults[-1] vs faults[0]: the value scenario stops at the first non-ok step, so faults has at most one element and [0]==[-1]. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:remove@return \"\"": "_oracle_kind's `return \"\"` -> implicit None: both are falsy, and the only consumer is `assert kind`, which fails identically for None or ''. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:sole-pass@return {**context, \"case\": case}": "The step's returned context is unused downstream: when() invokes the case via the closure (not context['case']), and then() is the last step, so a falsey/changed return leaves the oracle result unchanged. Verified across the battery.",
  "honest-test/src/honest_test/value_oracle.py:remove@return context": "The step's returned context is unused downstream: when() invokes the case via the closure (not context['case']), and then() is the last step, so a falsey/changed return leaves the oracle result unchanged. Verified across the battery.",
  "honest-test/src/honest_test/isolation.py:1->2@module_name, attr = path.rsplit(\".\", 1)": "Every I/O watch-list path is module.attr with exactly one dot, so rsplit('.', 1) and rsplit('.', 2) return the same two parts. No watch-list entry has two dots.",
  "honest-test/src/honest_test/isolation.py:remove@return None@io_finding": "Removing the trailing `return None` in io_finding (reached when the link touched no I/O or is a boundary) lets the function fall off its end, which returns None implicitly — identical to the explicit return.",
  "honest-test/src/honest_test/isolation.py:remove@return None@wrapper": "Removing the `return None` of the io recorder's wrapper (the patched I/O stand-in) lets the wrapper fall off its end, which returns None implicitly — identical; the stand-in still returns None and performs no I/O.",
  "honest-test/src/honest_test/scaffolding.py:string->empty@registry = register_step(registry, \"given\", _given_pattern(name, slots), given)": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the given step still resolves. (Emptying the pattern itself is caught — the step then goes unmatched.)",
  "honest-test/src/honest_test/scaffolding.py:string->empty@registry = register_step(registry, \"when\", f\"the {_humanize(name)} runs\", when)@scaffold_chain": "The emptied string is the step KIND argument to register_step; match_step matches purely on the pattern text and ignores the registered kind, so the when step still resolves. (Emptying the pattern f-string is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/scaffolding.py:string->empty@registry = register_step(registry, \"then\", \"the result is ok\", then_ok)@scaffold_chain": "The emptied string is the step KIND argument to register_step; match_step matches purely on the pattern text and ignores the registered kind, so the then step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/scaffolding.py:string->empty@registry = register_step(registry, \"then\", 'the result has a fault code \"{code}\"', then_fault)@scaffold_chain": "The emptied string is the step KIND argument to register_step; match_step matches purely on the pattern text and ignores the registered kind, so the then step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/scaffolding.py:remove@return context@then_ok": "then_ok's returned context is unused downstream: a then step is the last in a scenario, so a changed/None return leaves the scenario status unchanged (run_scenario reads the next step's context, and there is no next step).",
  "honest-test/src/honest_test/scaffolding.py:remove@return context@then_fault": "then_fault's returned context is unused downstream: a then step is the last in a scenario, so a changed/None return leaves the scenario status unchanged.",
  "honest-test/src/honest_test/http_steps.py:1->0@for part in _content_type(response).split(\";\")[1:]:": "The Content-Type media type at index 0 never carries a charset parameter, so folding it into the loop with [0:] instead of [1:] leaves the charset unchanged: it yields no charset name and is skipped. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@if attribute.type != \"attribute\":": "_element_attr's attribute-type guard is masked downstream: a non-attribute child of the open tag yields no attribute_name, so `attr_name == name` is False and it is skipped identically. The attribute walk is total. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return None": "Removing _element_attr's trailing `return None` (the named attribute is absent) lets the function fall off its end, returning None implicitly — identical.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@status_is": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@status_in_class": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@cookie_attribute": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@cookie_max_age": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@location_is": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@share_session": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@content_type_is": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@charset_is": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@header_equals": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@no_header": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@body_bytes_equal": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@body_json": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@body_html": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:remove@return context@sets_cookie": "honest-gherkin's run_step advances the context with `returned or context`, so a then-handler that returns the unchanged context and one that returns None are identical — removing the explicit `return context` leaves the same context. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:sole-pass@continue": "_element_attr's guard continue turned into pass: a non-attribute child falls through to the attribute_name lookup, which yields None, so it is skipped identically. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:string->empty@_EMPTY_REQUEST = {\"headers\": {}, \"cookies\": {}, \"body\": b\"\", \"content_type\": None}@<module>#3": "The emptied key is content_type in the empty-request default; nothing reads the default content_type (only request_content_type sets the real key), so emptying it changes no observed request. Equivalent.",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"given\", 'a request with Content-Type \"{mime}\"', request_content_type)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"given\", 'a request with body \"{content}\"', request_body)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"given\", 'a request with cookie \"{name}\" = \"{value}\"', request_cookie)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"given\", 'a request with header \"{name}\" = \"{value}\"', request_header)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", \"the response and the previous response share the same session cookie value\", share_session)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", \"the response body is JSON conforming to {schema}\", body_json)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", \"the response status is in {klass}\", status_in_class)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", \"the response status is {status:int}\", status_is)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response Content-Type is \"{mime}\"', content_type_is)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response body bytes equal \"{literal}\"', body_bytes_equal)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response body is HTML containing the selector \"{selector}\"', body_html)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response charset is \"{charset}\"', charset_is)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response cookie \"{name}\" has Max-Age {age:int}', cookie_max_age)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response cookie \"{name}\" has attribute \"{attribute}\"', cookie_attribute)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response has no header \"{name}\"', no_header)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response header \"{name}\" equals \"{value}\"', header_equals)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response location is \"{url}\"', location_is)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"then\", 'the response sets cookie \"{name}\" with value \"{value}\"', sets_cookie)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"when\", \"the previous response's Set-Cookie is used as the next request's Cookie\", reuse_set_cookie)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"when\", \"the session from the previous response is reused\", reuse_session)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"when\", 'a DELETE request is sent to \"{path}\"', delete)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"when\", 'a GET request is sent to \"{path}\"', get)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@registry = register_step(registry, \"when\", 'a POST request is sent to \"{path}\" with body \"{content}\"', post)@register_http_steps": "The emptied string is the step KIND argument to register_step; honest-gherkin's match_step matches purely on the pattern text and ignores the registered kind, so the step still resolves. (Emptying the pattern is caught — the step goes unmatched.)",
  "honest-test/src/honest_test/http_steps.py:string->empty@return {**context, \"previous_response\": context.get(\"response\"), \"response\": response, \"request\": _EMPTY_REQUEST}@send#3": "The emptied key is `request` in send()'s return; _request() falls back to the empty-request default when the request key is absent, so the request resets identically. Equivalent. (Emptying response or previous_response is caught by the assertion and share-session steps.)",
  "honest-test/src/honest_test/mutation.py:<-><=@if node.type in _NAMED_FUNCTIONS and node.start_byte <= offset < node.end_byte:": "The two forms differ only for an offset exactly equal to a function's end_byte, and no mutation site can sit there: a function_definition ends at its trailing newline, so the next construct starts at end_byte + 1, and a newline is never the start of a mutated node. Verified empirically over all 128 module sources — zero sites land on a function's end byte (against 937 that land on a start byte, which is why the opening edge is exercised by a law instead). Equivalent."
}
