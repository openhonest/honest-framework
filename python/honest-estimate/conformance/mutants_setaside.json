{
  "honest-estimate/src/honest_estimate/estimate.py:0->1@from_role = _MOVEMENT_BY_ROLE.get(f[\"role\"], 0)": "Equivalent: the .get default is unreachable. Every elementary process carries a role in {boundary_in, boundary_out, orchestrator, fn} and every side effect a direction in {reads, writes, reads_writes} \u2014 both closed vocabularies that are keys of the mapping \u2014 so the default arm is never taken and its value cannot affect any output.",
  "honest-estimate/src/honest_estimate/estimate.py:0->-1@from_role = _MOVEMENT_BY_ROLE.get(f[\"role\"], 0)": "Equivalent: the .get default is unreachable. Every elementary process carries a role in {boundary_in, boundary_out, orchestrator, fn} and every side effect a direction in {reads, writes, reads_writes} \u2014 both closed vocabularies that are keys of the mapping \u2014 so the default arm is never taken and its value cannot affect any output.",
  "honest-estimate/src/honest_estimate/estimate.py:0->1@from_effects = sum(_MOVEMENT_BY_EFFECT.get(se[\"direction\"], 0) for se in f[\"side_effects\"])": "Equivalent: the .get default is unreachable. Every elementary process carries a role in {boundary_in, boundary_out, orchestrator, fn} and every side effect a direction in {reads, writes, reads_writes} \u2014 both closed vocabularies that are keys of the mapping \u2014 so the default arm is never taken and its value cannot affect any output.",
  "honest-estimate/src/honest_estimate/estimate.py:0->-1@from_effects = sum(_MOVEMENT_BY_EFFECT.get(se[\"direction\"], 0) for se in f[\"side_effects\"])": "Equivalent: the .get default is unreachable. Every elementary process carries a role in {boundary_in, boundary_out, orchestrator, fn} and every side effect a direction in {reads, writes, reads_writes} \u2014 both closed vocabularies that are keys of the mapping \u2014 so the default arm is never taken and its value cannot affect any output.",
  "honest-estimate/src/honest_estimate/estimate.py:0->-1@if sets.get(type_name, 0) > 0:@_bits_of": "Equivalent: this shifts the .get default from 0 to -1 for an absent type name. Both 0 and -1 are <= 0, so the guard is false either way and the slot takes the identical open branch; only a present key (which never uses the default) affects the result.",
  "honest-estimate/src/honest_estimate/estimate.py:0->-1@if vocabs.get(type_name, 0) > 0:@_bits_of": "Equivalent: this shifts the .get default from 0 to -1 for an absent type name. Both 0 and -1 are <= 0, so the guard is false either way and the slot takes the identical open branch; only a present key (which never uses the default) affects the result.",
  "honest-estimate/src/honest_estimate/estimate.py:string->empty@_MOVEMENT_BY_ROLE = {\"boundary_in\": 1, \"boundary_out\": 1, \"orchestrator\": 0, \"fn\": 0}@<module>#2": "Equivalent: blanks the \"orchestrator\" key, whose value is 0. An orchestrator role then misses the mapping and takes the default 0 \u2014 the same value it mapped to \u2014 so no count changes.",
  "honest-estimate/src/honest_estimate/estimate.py:string->empty@_MOVEMENT_BY_ROLE = {\"boundary_in\": 1, \"boundary_out\": 1, \"orchestrator\": 0, \"fn\": 0}@<module>#3": "Equivalent: blanks the \"fn\" key, whose value is 0. A fn role then misses the mapping and takes the default 0 \u2014 the same value \u2014 so no count changes.",
  "honest-estimate/src/honest_estimate/estimate.py:key->sibling@_MOVEMENT_BY_ROLE = {\"boundary_in\": 1, \"boundary_out\": 1, \"orchestrator\": 0, \"fn\": 0}@<module>#2": "Equivalent (residual); see estimate.py."
}