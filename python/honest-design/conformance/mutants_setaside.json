{
  "honest-design/src/honest_design/reader.py:0->-1@\"layer\": (groups.get(\"layer\") or [\"\"])[0],": "equivalent: a module has at most one layer and the fallback list has exactly one element, so [0] and [-1] address the same element.",
  "honest-design/src/honest_design/reader.py:0->-1@body = _children(node, \"chain_body\")[0]": "equivalent: a chain_decl has exactly one chain_body child, so [0] and [-1] address the same element.",
  "honest-design/src/honest_design/reader.py:0->-1@body = _children(node, \"flow_body\")[0]": "equivalent: a flow_decl has exactly one flow_body child, so [0] and [-1] address the same element.",
  "honest-design/src/honest_design/reader.py:0->-1@params_node = _children(signature, \"params\")[0]": "equivalent: a function has exactly one signature and a signature exactly one params node, so [0] and [-1] address the same element.",
  "honest-design/src/honest_design/reader.py:0->-1@signature = _children(node, \"signature\")[0]": "equivalent: a function_decl has exactly one signature child, so [0] and [-1] address the same element.",
  "honest-design/src/honest_design/reader.py:1->2@parts = _unquote(_field_text(node, \"path\", source)).split(\" \", 1)": "equivalent: a route path is 'METHOD /path' with a single space; split(' ', 1) and split(' ', 2) yield the same parts, so the limit is unobservable on valid input.",
  "honest-design/src/honest_design/result.py:False->True@class Result(TypedDict, total=False):": "equivalent: total=False on a TypedDict is a static-typing directive with no runtime effect (TypedDict performs no validation at construction), so flipping it to True cannot change any value the suite observes.",
  "honest-design/src/honest_design/validator.py:0->-1@if len(params) != 1 or params[0][\"type\"] != fields[entry[\"projection\"]]:": "equivalent: the `len(params) != 1` test short-circuits the `or`, so the index is only reached when params holds exactly one element, and [0] and [-1] address it alike. Verified by reading the guard, not by the mutant surviving.",
  "honest-design/src/honest_design/validator.py:0->-1@if budget == 0 or name not in aliases:": "equivalent: the budget is the number of aliases in the module, which already exceeds the longest chain that leads anywhere, so stopping at -1 instead of 0 takes one more step and reaches nothing the original could not; a cycle still ends when the budget runs out."
}
