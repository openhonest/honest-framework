{
 "honest-check/src/honest_check/suppression.py:remove@directives.sort(key=lambda directive: directive[0])": "tree-sitter yields comment nodes in source (== line) order, so _collect_directives already builds the list in line order; sorting by line is identity. Battery-verified that walk order equals line order across nested functions, classes, if/else, blank lines, and trailing comments, so removing the sort cannot change the output.",
 "honest-check/src/honest_check/suppression.py:0->-1@directives.sort(key=lambda directive: directive[0])": "Sorting by the rules frozenset (index -1) instead of the line (index 0): the input is already in line order (battery-verified), and frozenset '<' is the subset relation — disjoint or equal rule sets compare False both ways, so list.sort is stable and preserves the existing line order. The output is identical to sorting by line.",
 "honest-check/src/honest_check/startup.py:string->empty@_ON_ERROR = {\"warn\": _on_warn, \"raise\": _on_raise, \"halt\": _on_halt}": "Emptying the \"warn\" key. startup_check dispatches via _ON_ERROR.get(on_error, _on_warn), whose default IS _on_warn, so on_error='warn' resolves to _on_warn whether the key is present or absent. Behaviour is identical.",
 "honest-check/src/honest_check/startup.py:string->empty@def startup_check(paths: list[str], on_error: str = \"warn\", severity: str = \"error\") -> None:": "Emptying the on_error default ('warn' -> ''). A caller who omits on_error then gets ''; _ON_ERROR.get('', _on_warn) falls to the default _on_warn — exactly what 'warn' resolves to. Behaviour is identical.",
 "honest-check/src/honest_check/startup.py:key->sibling@_ON_ERROR = {\"warn\": _on_warn, \"raise\": _on_raise, \"halt\": _on_halt}": "The surviving swap moves the \"warn\" key onto a sibling name. Verified exhaustively that only warn->sibling swaps preserve behaviour (raise->* and halt->* are all caught by the warn/raise/halt assertions): the sibling's own value wins the duplicate-key collision so raise/halt are unaffected, and get('warn') falls to the default _on_warn. Behaviour is identical."
}
