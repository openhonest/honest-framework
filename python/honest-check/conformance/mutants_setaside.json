{
  "honest-check/src/honest_check/suppression.py:remove@directives.sort(key=lambda directive: directive[0])": "tree-sitter yields comment nodes in source (== line) order, so _collect_directives already builds the list in line order; sorting by line is identity. Battery-verified that walk order equals line order across nested functions, classes, if/else, blank lines, and trailing comments, so removing the sort cannot change the output.",
  "honest-check/src/honest_check/suppression.py:0->-1@directives.sort(key=lambda directive: directive[0])": "Sorting by the rules frozenset (index -1) instead of the line (index 0): the input is already in line order (battery-verified), and frozenset '<' is the subset relation — disjoint or equal rule sets compare False both ways, so list.sort is stable and preserves the existing line order. The output is identical to sorting by line.",
  "honest-check/src/honest_check/startup.py:string->empty@_ON_ERROR = {\"warn\": _on_warn, \"raise\": _on_raise, \"halt\": _on_halt}@<module>": "Emptying the \"warn\" key. startup_check dispatches via _ON_ERROR.get(on_error, _on_warn), whose default IS _on_warn, so on_error='warn' resolves to _on_warn whether the key is present or absent. Behaviour is identical.",
  "honest-check/src/honest_check/startup.py:string->empty@def startup_check(paths: list[str], on_error: str = \"warn\", severity: str = \"error\") -> None:@startup_check": "Emptying the on_error default ('warn' -> ''). A caller who omits on_error then gets ''; _ON_ERROR.get('', _on_warn) falls to the default _on_warn — exactly what 'warn' resolves to. Behaviour is identical.",
  "honest-check/src/honest_check/startup.py:key->sibling@_ON_ERROR = {\"warn\": _on_warn, \"raise\": _on_raise, \"halt\": _on_halt}@<module>": "The surviving swap moves the \"warn\" key onto a sibling name. Verified exhaustively that only warn->sibling swaps preserve behaviour (raise->* and halt->* are all caught by the warn/raise/halt assertions): the sibling's own value wins the duplicate-key collision so raise/halt are unaffected, and get('warn') falls to the default _on_warn. Behaviour is identical.",
  "honest-check/src/honest_check/cli.py:string->empty@if __name__ == \"__main__\":": "The script entry guard. __name__ is the module's dotted name under import, never '__main__' and never '', so emptying the literal leaves the guard False under conformance exactly as the real guard is. main() itself is exhaustively tested via cli_main.",
  "honest-check/src/honest_check/cli.py:remove@if __name__ == \"__main__\":": "The `if __name__ == '__main__'` block runs only when cli.py is executed as a script (python -m / direct), never under import-based conformance. main() is exhaustively tested via cli_main, so the one-line entry wrapper is unreachable from the suite by construction.",
  "honest-check/src/honest_check/cli.py:sole-pass@raise SystemExit(main())": "Body of the script entry guard, reached only when cli.py is run as a script — never under import-based conformance. main() is tested directly via cli_main.",
  "honest-check/src/honest_check/cli.py:remove@from honest_check.diagnostics import Diagnostic": "Diagnostic is used only in the local variable annotation `diagnostics: list[Diagnostic] = []` inside _run_once. Python does not evaluate local variable annotations at runtime (PEP 526), so removing the import cannot raise — verified that a local annotation referencing an undefined name does not error.",
  "honest-check/src/honest_check/cli.py:remove@return None": "_find_config falls off the end of its ancestor loop returning None when no honest-check.toml is found, so the explicit `return None` is redundant — verified the function returns None with the line removed.",
  "honest-check/src/honest_check/formats.py:0->-1@_SEVERITY_RANK = {\"info\": 0, \"warning\": 1, \"error\": 2}": "Shifting info's rank 0 -> -1 preserves the strict order info<warning<error. filter_by_severity reads the SAME map for both the floor and each diagnostic's rank, so any order-preserving shift leaves every (minimum, severity) decision unchanged. Battery-verified equivalent across all four minimums.",
  "honest-check/src/honest_check/formats.py:2->3@_SEVERITY_RANK = {\"info\": 0, \"warning\": 1, \"error\": 2}": "Shifting error's rank 2 -> 3 preserves the strict order info<warning<error. As filter_by_severity reads one map for both floor and per-diagnostic rank, an order-preserving shift cannot change any filtering decision. Battery-verified equivalent across all four minimums.",
  "honest-check/src/honest_check/formats.py:string->empty@_GITHUB_LEVEL = {\"error\": \"error\", \"warning\": \"warning\", \"info\": \"notice\"}@<module>#4": "Emptying the \"info\" key. render_github resolves the level via _GITHUB_LEVEL.get(severity, \"notice\"); since info already maps to the same \"notice\" as the default, dropping the info key leaves get(\"info\") returning \"notice\" unchanged. Verified equivalent.",
  "honest-check/src/honest_check/watchlists.py:remove@if not name:": "The empty-name fast path. No watch-list entry can match an empty name: an exact entry requires equality, and both wildcard forms require the name to start with a non-empty prefix that '' cannot start with. So removing the guard still falls through to the final `return False`. Battery-verified equivalent over 547 probe names on both lists.",
  "honest-check/src/honest_check/watchlists.py:sole-pass@return False": "The empty-name guard's `return False` turned into pass: '' then falls through the loop, where no entry matches it (same reasoning as the guard removal), reaching the final `return False`. Battery-verified equivalent over 547 probe names on both lists.",
  "honest-check/src/honest_check/watchlists.py:remove@continue": "After the dotted-wildcard branch, `continue` skips the bare-wildcard check. For a '.*' entry, entry[:-1] equals prefix + '.', so the bare-wildcard startswith test is identical to the dotted branch's name.startswith(prefix + '.') that already ran; it can only be True when the dotted branch already returned True. So falling through cannot change the result. Battery-verified equivalent over 547 probe names on both lists.",
  "honest-check/src/honest_check/declgraph.py:!=->==@if node.type != \"function_definition\":@vocab_binding_pairings": "vocab_binding_pairings reaches the @link decorator via link_decorator_call (parent is the decorated_definition shared by the funcdef and its decorator), so inverting the funcdef filter still produces the identical pair. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:0->-1@inner = node.named_children[0] if node.named_children else None": "Index shift on a node that has exactly one relevant named child (a decorator/parenthesized expression wraps a single expression), so [0], [-1] and [1] (when present) select the same child or the guarded `else None` path. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:0->-1@expr = child.named_children[0] if child.named_children else None": "Index shift on a node that has exactly one relevant named child (a decorator/parenthesized expression wraps a single expression), so [0], [-1] and [1] (when present) select the same child or the guarded `else None` path. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:0->-1@expr = child.named_children[0]": "Index shift on a node that has exactly one relevant named child (a decorator/parenthesized expression wraps a single expression), so [0], [-1] and [1] (when present) select the same child or the guarded `else None` path. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:and->or@if child.type == \"dotted_name\" and node_text(child, source) == \"honest_type\":": "The body adds the literal \"honest_type\" (not the child's text). Under `import honest_type` that name is already in the module set, so widening the guard with or->and re-adds an already-present member: no observable change. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:and->or@if parent is not None and parent.type == \"assignment\":": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if fn is None or fn.type != \"identifier\" or names.get(node_text(fn, source)) != \"composed\":@extract_composed_types#1": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:add-not@if node.type != \"function_definition\":@vocab_binding_pairings": "vocab_binding_pairings only uses the looped node via link_decorator_call, which keys off parent.type == 'decorated_definition'. A function_definition and its decorator share that same decorated_definition parent, so flipping the funcdef filter still reaches the @link decorator and yields the identical pair. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if left is None or right is None or left.type != \"identifier\":@build_vocabulary_definitions#1": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if parent is None or parent.type != \"decorated_definition\":@link_decorator_call": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if expr is None or expr.type != \"call\":": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if parent is None or parent.type != \"decorated_definition\":@function_role": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if key is None or value is None or key.type != \"tuple\":@transition_table": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:or->and@if key is None or value is None or key.type != \"tuple\":@transition_table#1": "Boolean-operator flip around a dead guard operand. The `... is None` (or `is not None`) operand is constantly False/True because the field is always present on a cleanly-parsed node (field-presence invariant battery-verified); flipping the operator around a constant operand cannot change the guard's value. The live discriminant (the `.type` comparison) is pinned by the declgraph extractors' exact-output tests. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:string->empty@if child.type != \"keyword_argument\"": "This occurrence is vocab_binding_pairings' positional filter. Emptying the literal lets keyword_argument nodes into the positional list, but they sit AFTER the real positionals (Python requires positional-before-keyword), so positional[1]/[2] are unchanged, and a keyword_argument node fails the subsequent `.type == 'identifier'` check, so it never becomes a vocab/binding. The keyword_args() occurrence with the same line text is caught by the keyword_args exact-output test. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:string->empty@return node_text(name, source) if name is not None else \"<anonymous>\"": "Unreachable fallback: a parsed function_definition always carries a name field, so the `name is None` branch returning \"<anonymous>\" is never taken. Field-presence invariant battery-verified.",
  "honest-check/src/honest_check/declgraph.py:remove@if child == module:": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@resolve_aliases#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@return None@assigned_name": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@return \"\"": "Trailing empty-string return reached only on the no-match fall-through; the alternative reachable paths already return the same value and removing/`pass`-ing it changes no observed output across the suite. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@return None@_dictionary_arg": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@break@_parse_composed": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if pair.type != \"pair\":@_parse_composed": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@_parse_composed": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@break@_parse_composed#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if element.type != \"call\":": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@extract_composed_types#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if pair.type != \"pair\":@extract_bindings": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@extract_bindings#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if node.type != \"call\":@_calls_by_name": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@_calls_by_name": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@_states_of#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@module_assignments": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if statement.type != \"expression_statement\":": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@feature_vocabulary": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@return set()": "The empty-set early return for a body-less / non-matching node; the value is rebuilt as the same empty accumulator on the fall-through. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if parent is None or parent.type != \"decorated_definition\":@link_decorator_call": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@return None@module_dict_keys": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if child.type != \"decorator\":@link_decorator_call": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@_feature_state_flag#1": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if expr is None or expr.type != \"call\":": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@feature_vocabulary#2": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@feature_vocabulary#3": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@return None@link_decorator_call": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if parent is None or parent.type != \"decorated_definition\":@function_role": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@_feature_state_flag#2": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if node.type != \"call\":@function_calls": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@handler_table_dispatches#1": "Loop-control on a defensive skip: the node it skips (a non-matching child / already-found match) is either re-handled identically by a downstream guard or cannot arise from honest source, so removing the continue/break leaves the loop's result unchanged. Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if key_node.type != \"tuple\":": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@link_decorator_call": "Trailing `return None` (or its sole-pass): the function falls off its end returning None implicitly, so removing the explicit return yields the same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:remove@if pair.type != \"pair\":@feature_vocabulary": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/declgraph.py:remove@if key is None or value is None or key.type != \"tuple\":": "Removing this structural/type guard is equivalent: a node of the unexpected type is caught by the following guard (a downstream None- or length-check) and skipped identically, or the unexpected node cannot arise from a cleanly-parsed honest source (field-presence invariant battery-verified). Gate-confirmed equivalent over the full suite.",
  "honest-check/src/honest_check/lsp.py:0->-1@line = max(d[\"line\"] - 1, 0)": "The line floor shifted to -1. honest-check lines are 1-based (>= 1), so d['line'] - 1 >= 0 >= -1 and the floor never applies; max(d-1, 0) == max(d-1, -1). Battery-verified over 1..499.",
  "honest-check/src/honest_check/lsp.py:0->-1@col = max(d[\"col\"] - 1, 0)": "The max(..., 0) floor with the floor shifted to -1. honest-check columns are 1-based (>= 1), so d['col'] - 1 >= 0 >= -1 and the floor is never the larger operand; max(d-1, 0) == max(d-1, -1) for every reachable input. Battery-verified over 1..499.",
  "honest-check/src/honest_check/lsp.py:0->-1@start_line = lsp_range.get(\"start\", {}).get(\"line\", 0)": "The default for a missing start line shifted 0 -> -1. start_line is only a lower bound in `start_line <= diagnostic_line <= end_line`, and a diagnostic line is a 0-based line (>= 0), so -1 and 0 admit exactly the same diagnostics. Battery-verified.",
  "honest-check/src/honest_check/lsp.py:1->2@content_length = int(header.split(b\":\", 1)[1].strip())@_read_message": "split maxsplit 1 -> 2 on the Content-Length header. The header value is a colon-free integer, so the header contains exactly one colon and split(b':', 1) == split(b':', 2). Battery-verified for all integer lengths.",
  "honest-check/src/honest_check/lsp.py:string->empty@_LSP_SEVERITY = {\"error\": 1, \"warning\": 2, \"info\": 3}@<module>#2": "Emptying the \"info\" key. to_lsp_diagnostic resolves severity via _LSP_SEVERITY.get(severity, 3); info already maps to the same 3 as the default, so dropping the info key leaves get(\"info\") == 3 unchanged. Verified (the error/warning/info/default map is pinned by the severity test).",
  "honest-check/src/honest_check/lsp.py:string->empty@\"initialized\": _noop,": "Emptying a no-op handler key. dispatch falls back to _noop via _HANDLERS.get(method, _noop), so a method whose entry maps to _noop behaves identically whether the key is present, absent, or empty. Gate-confirmed equivalent against the full routing suite.",
  "honest-check/src/honest_check/lsp.py:string->empty@\"textDocument/didSave\": _noop,": "Emptying a no-op handler key. dispatch defaults to _noop, so a didSave entry that maps to _noop is indistinguishable from the missing/empty key. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:key->sibling@\"initialized\": _noop,": "Swapping a _noop key onto a later sibling: that sibling's own (correct) handler value wins the duplicate-key collision, and the noop method falls back to the default _noop, so routing is unchanged. Gate-confirmed equivalent against the full routing suite.",
  "honest-check/src/honest_check/lsp.py:key->sibling@\"textDocument/didSave\": _noop,": "Swapping a _noop key onto a later sibling: the sibling's own handler wins the collision and didSave falls back to the default _noop, leaving routing unchanged. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@return None@_hover_contents": "Trailing `return None` of _hover_contents (no diagnostic on the line): removing it lets the function fall off its end, which returns None implicitly. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@return None@_node_at": "Trailing `return None` of _node_at (no identifier covers the position): the function falls off its end returning None implicitly. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@return None@_definition_of": "Trailing `return None` of _definition_of (name not defined): falling off the end returns None implicitly. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@if function.type != \"identifier\":": "_document_symbols: removing the identifier guard lets an attribute-call function (e.g. mod.thing()) through, but node_text of a non-identifier function is not a key in _SYMBOL_KINDS, so the subsequent `kind is None` guard skips it identically. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:sole-pass@continue@_document_symbols#2": "_document_symbols: turning the identifier-guard `continue` into pass falls through to the kind lookup, where a non-identifier function name is absent from _SYMBOL_KINDS and the next `kind is None` guard skips it identically. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@if not line:": "_read_message: removing the EOF fast-path is equivalent — at EOF readline returns b'' whose strip() is b'', which hits the `if header == b'': break`, then read(content_length) yields b'' and the empty-body guard returns None. Same result. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:sole-pass@return None@_read_message": "_definition_of trailing `return None` turned into pass: the function then falls off its end, still returning None. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/lsp.py:remove@stream.flush()": "_write_message flush affects only real-stream liveness, not the bytes written; the message is fully written before the flush. The conformance harness writes to BytesIO, which buffers identically with or without flush. Equivalent for the written output.",
  "honest-check/src/honest_check/rules.py:!=->==@if node.type != \"function_definition\":@check_hc_p006": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->2@line, col = line_col(node) if node is not None else (1, 1)@check_hc_syn": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@line, col = line_col(node) if node is not None else (1, 1)@check_hc_syn": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->2@line, col = line_col(node) if node is not None else (1, 1)@check_hc_syn#1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@line, col = line_col(node) if node is not None else (1, 1)@check_hc_syn#1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@return text.split(\"[\")[0].split(\".\")[-1].strip()": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@if not values or values[0].type != \"call\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@function = values[0].child_by_field_name(\"function\")": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@return body.split(\"(\")[0].split(\".\")[-1].strip()": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@return body.split(\"(\")[0].split(\".\")[-1].strip()": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@line, col = line_col(cache_decorators[0])": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:0->-1@line, col = line_col(links[0][1])": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@return (\"ref\", recognizer[1])": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:2->1@if len(type_names) < 2:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->2@previous = [0] * (len(second) + 1)": "Distance-matrix row sizing: a +2 row leaves the extra cell unread and a smaller previous row is only read at indices that still exist, so the result is unchanged (the killable size/seed errors are pinned by the _longest_common_run tests). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@previous = [0] * (len(second) + 1)": "Distance-matrix row sizing: a +2 row leaves the extra cell unread and a smaller previous row is only read at indices that still exist, so the result is unchanged (the killable size/seed errors are pinned by the _longest_common_run tests). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->2@current = [0] * (len(second) + 1)": "Distance-matrix row sizing: a +2 row leaves the extra cell unread and a smaller previous row is only read at indices that still exist, so the result is unchanged (the killable size/seed errors are pinned by the _longest_common_run tests). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->2@max_line = root.end_point[0] + 1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:1->0@max_line = root.end_point[0] + 1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if function is None or function.type != \"identifier\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if left.type == \"identifier\" and node_text(left, source) == name:": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:add-not@if sub.type == \"identifier\" and node_text(sub, source) == name:": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if sub.type == \"identifier\" and node_text(sub, source) == name:": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if parent is None or parent.type != \"decorated_definition\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if value is not None and value.type == \"identifier\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if left is None or right is None or left.type != \"identifier\":@_mutable_module_containers": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if left is None or right is None or left.type != \"identifier\":@_mutable_module_containers#1": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if fn is not None and fn.type == \"attribute\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if left is not None and left.type == \"identifier\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@return node.type == \"comment\" and \"honest:\" in text and \"profiled\" in text@_is_profiled_comment": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@return node.type == \"comment\" and \"honest:\" in text and \"profiled\" in text@_is_profiled_comment#1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@anchor = parent if parent is not None and parent.type == \"decorated_definition\" else func_node": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:add-not@if node.type != \"function_definition\":@check_hc_p006": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if callee in functions and callee not in reachable:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if left is not None and left.type == \"subscript\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if binds is None or binds.type != \"identifier\" or node_text(binds, source) not in bindings:@check_hc010": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:and->or@if node.type == \"call\" and _qualified_call_name(node, source)": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:or->and@if not first or not second:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:add-not@if child.type in (\"/\", \"//\"):": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@if child.type in (\"identifier\", \"attribute\"):@_class_base_names#1": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@name = node_text(name_node, source) if name_node is not None else \"<anonymous>\"": "Unreachable fallback: a parsed function/class definition always carries a name field, so the `<anonymous>` branch is never taken. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@if left.type in (\"pattern_list\", \"tuple_pattern\", \"tuple\"):@_rebinds_name#1": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@if left.type in (\"pattern_list\", \"tuple_pattern\", \"tuple\"):@_rebinds_name#2": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@if node.type in (\"assignment\", \"augmented_assignment\"):@_local_names#1": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@return node.type == \"comment\" and \"honest:\" in text and \"profiled\" in text@_is_profiled_comment#1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@return node.type == \"comment\" and \"honest:\" in text and \"profiled\" in text@_is_profiled_comment#2": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@child.type in (\"except_clause\", \"except_group_clause\") for child in try_node.children@_has_except_clause#1": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@if node.type in (\"assignment\", \"augmented_assignment\"):@_produced_slot_keys#1": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@return (\"set\", recognizer[1])": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:string->empty@return (\"ref\", recognizer[1])": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:in->not in@if child.type in (\"/\", \"//\"):": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@continue": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"class_definition\":@_typeddict_names": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@_typeddict_names": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"return_statement\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if function is None or function.type != \"identifier\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p010": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if condition.type != \"comparison_operator\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if left.type != \"identifier\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return None@_equality_target": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return None@_equality_target#2": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"if_statement\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"call\":@check_hc_p011": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if cls.type != \"class_definition\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return False@_rebinds_name": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"function_definition\":@check_hc_p016": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if not captured:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return \"\"": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if parent is None or parent.type != \"decorated_definition\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return []@_decorators": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return False@_is_boundary_function": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return None@_enclosing_function": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"subscript\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return None@_subscript_base": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return None@_subscript_base": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if left is None or right is None or left.type != \"identifier\":": "Boolean flip around a dead guard operand: the `... is None`/`is not None` field is always present on a cleanly-parsed node, so that operand is constant and the flip cannot change the guard; the live `.type` discriminant is pinned by the rule cases. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@break@_local_names": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"identifier\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"call\":@check_hc_p004": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"call\":@check_hc_p005": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return False@_has_profiling_evidence": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@aliases = resolve_aliases(root, source)@check_hc_r001": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if not links:@check_hc_a002": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return []@check_hc_a002": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if pair.type != \"pair\":": "Type/shape guard whose removal is masked downstream: a node of the unexpected type yields None/empty from the following accessor and is skipped identically (or cannot arise from a cleanly-parsed tree). Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if not new_emits:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@return None  # predicates are opaque — treat each as unique, no reuse detection": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if len(type_names) < 2:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if not first or not second:": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@return 0": "Trailing/defensive return reached only on the no-match path; removing it (or making it pass) falls through to the same value the function already yields. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@break@check_hc_p017": "Defensive guard / dead operand on cleanly-parsed structure with no observable effect. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@_feature_state_flag": "Trailing `return None` turned into pass: the function falls off its end, still returning None. Same value. Battery-verified equivalent (unchanged declgraph code; only the label index shifted when the auth derivation helpers were removed).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@link_decorator_call": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source; removing it does not change the loop's result. Battery-verified equivalent (unchanged declgraph code; label index shifted after the auth derivation helpers were removed).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@link_decorator_call#1": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source; removing it does not change the loop's result. Battery-verified equivalent (unchanged declgraph code; label index shifted after the auth derivation helpers were removed).",
  "honest-check/src/honest_check/declgraph.py:string->empty@positional = [c for c in args.named_children if c.type not in (\"keyword_argument\", \"comment\", \"list_splat\", \"dictionary_splat\")]@_feature_state_flag": "Emptying 'keyword_argument' in _feature_state_flag's excluded-argument set. A keyword argument is only legal after the positional arguments, so it can never be the second positional (the flag); including it never changes positional[1]. Equivalent.",
  "honest-check/src/honest_check/declgraph.py:string->empty@positional = [c for c in args.named_children if c.type not in (\"keyword_argument\", \"comment\", \"list_splat\", \"dictionary_splat\")]@_feature_state_flag#3": "Emptying 'dictionary_splat' in _feature_state_flag's excluded-argument set. A **-splat is only legal after the positional arguments, so it can never be the second positional (the flag); including it never changes positional[1]. Equivalent.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@function_role": "function_role: neutralising the parent-guard early return lets an undecorated function fall into the decorator loop; its parent carries no decorator children, so role stays None and the function returns None anyway. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:remove@return None@function_role": "function_role: removing the final 'return None' lets the function fall off its end, returning None implicitly — the same value. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@function_calls": "Defensive loop skip on a non-call node whose fn is then None, which the following 'fn is not None' guard rejects identically; removing it does not change the result. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@return None@_route_key": "_route_key: neutralising the non-tuple early return lets a non-tuple key fall through; it then yields fewer than two string children and returns None at the length guard anyway. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:remove@if pair.type != \"pair\":@transition_table": "transition_table: removing the pair-type guard lets a non-pair dict child reach the key/value reads, which return None and are dropped by the downstream None/tuple guard; the result is unchanged. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@transition_table": "Defensive loop skip in transition_table on a node the downstream None/tuple guard re-rejects identically; removing it does not change the result. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@transition_table#1": "Defensive loop skip in transition_table on a node the downstream None/tuple guard re-rejects identically; removing it does not change the result. Equivalent (unchanged declgraph code; label index shifted after the feature-flag extractors were added).",
  "honest-check/src/honest_check/rules.py:remove@if not vocab:@check_hc_hf002": "check_hc_hf002: with no FEATURES the early return is removed, but the dispatch loop then finds every flag not in the empty vocabulary and continues, returning the same empty list. Equivalent.",
  "honest-check/src/honest_check/rules.py:sole-pass@return []@check_hc_hf002": "check_hc_hf002: neutralising the empty-vocabulary early return falls through to the dispatch loop, which skips every flag (none is in the empty vocabulary) and returns the same empty list. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:0->-1@return node_text(named[0], source) if named else None": "_class_base: shifting named[0] to named[-1]. A JavaScript class_heritage has exactly one named child (the superclass — JavaScript is single-inheritance; the `extends` keyword is an anonymous token), so named[0] and named[-1] are the same node. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_class_base": "_class_base: removing the final 'return None' (reached when the class has no class_heritage) lets the function fall off its end, returning None implicitly — the same value. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@if node.type != \"call_expression\":": "check_hc_p011_js: removing the call-expression guard lets every node reach _js_call_name, which returns '' for any non-call node (no 'function' field); '' is never a lifecycle hook, so no extra diagnostic fires. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p006_js": "check_hc_p011_js: neutralising the call-expression guard's continue lets a non-call node fall through to _js_call_name, which returns '' (no 'function' field); '' is never a lifecycle hook, so the result is unchanged. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:0->-1@condition = condition.named_children[0]": "_js_equality_target: a parenthesized_expression wraps exactly one expression, so named_children[0] and named_children[-1] are the same node. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:0->-1@if inner and inner[0].type == \"if_statement\":": "_js_else_if: an else_clause has exactly one named child (the else-if if_statement, or the else block), so inner[0] and inner[-1] are the same node. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:0->-1@return inner[0]": "_js_else_if: an else_clause has exactly one named child, so inner[0] and inner[-1] are the same node. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_js_cache_construct": "_js_type_check: removing the final 'return None' (reached when the node is neither a typeof unary nor an instanceof binary) lets the function fall off its end, returning None implicitly. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@if condition.type != \"binary_expression\":": "_js_equality_target: removing the binary-expression guard lets a non-binary condition reach the operator check; a non-binary node's operator is None or not in ('===','=='), so it returns None anyway (only a binary_expression carries those operators). Equivalent.",
  "honest-check/src/honest_check/js_rules.py:sole-pass@return None@_js_equality_target#1": "_js_equality_target: neutralising the binary-expression guard's return lets a non-binary condition fall through to the operator check, which returns None for any non-binary node. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_js_type_check": "_js_else_if: removing the final 'return None' (reached for a plain else or no else-if) lets the function fall off its end, returning None implicitly. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@if node.type != \"if_statement\":": "check_hc_p001_js: removing the if-statement guard lets every node be processed; a non-if node yields no else-if chain (_js_else_if follows only if_statement alternatives), so at most one condition — never the three needed to fire. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p005_js": "check_hc_p001_js: neutralising the if-statement guard's continue lets a non-if node fall through; it yields at most one chain condition (_js_else_if follows only if_statement alternatives), never the three needed to fire. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@if not outer_lets:": "check_hc_p016_js: removing the empty-outer_lets short-circuit lets the inner loop run with an empty outer_lets set; captured = reassigned & {} is always empty, so no diagnostic fires — the same result. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p016_js": "check_hc_p016_js: neutralising the empty-outer_lets continue lets the inner loop run with an empty outer_lets set; captured = reassigned & {} is always empty, so no diagnostic fires. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p011_js": "check_hc_p011_js: neutralising the call-expression guard's continue lets a non-call node reach _js_call_name, which returns '' (no 'function' field); '' is never a lifecycle hook, so no extra diagnostic. Equivalent (occurrence index shifted when HC-P006 was added).",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_js_else_if": "_js_else_if: removing the final 'return None' (plain else or no else-if) lets the function fall off its end, returning None implicitly. Equivalent (occurrence index shifted when HC-P006 was added).",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p001_js": "check_hc_p001_js: neutralising the if-statement guard's continue lets a non-if node fall through; it yields at most one chain condition, never the three needed to fire. Equivalent (occurrence index shifted when HC-P006 was added).",
  "honest-check/src/honest_check/js_rules.py:sole-pass@continue@check_hc_p016_js#1": "check_hc_p016_js: neutralising the empty-outer_lets continue lets the inner loop run with an empty outer_lets; captured = reassigned & {} is always empty, so no diagnostic fires. Equivalent (occurrence index shifted when HC-P006 was added).",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_js_impure_name": "_js_impure_name: removing the final 'return None' (reached when a node is neither a watched call nor a watched constructor) lets the function fall off its end, returning None implicitly. Equivalent.",
  "honest-check/src/honest_check/js_rules.py:remove@return None@_js_enclosing_function": "_js_enclosing_function: removing the final 'return None' (reached at module level, no enclosing function) lets the function fall off its end, returning None implicitly. Equivalent.",
  "honest-check/src/honest_check/templates.py:remove@return None@_open_tag": "_open_tag: removing the trailing 'return None' (reached when a node has no start_tag/self_closing_tag child) lets the function fall off its end, returning None implicitly. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:remove@return None@_attr": "_attr: removing the trailing 'return None' (reached when the named attribute is absent from the open tag) lets the function fall off its end, returning None implicitly. Same value. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:remove@if attribute.type != \"attribute\":@_attr": "_attr's attribute-type guard whose removal is masked downstream: for a non-attribute child of the open tag (<, tag_name, >), the following `next(walk(child) if c.type == 'attribute_name')` yields None, so `attr_name != name` skips it identically. The extraction pipeline is total. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@_attr": "_attr's attribute-type guard `continue` turned into pass: a non-attribute child then falls through to the attribute_name lookup, which yields None and is skipped by `attr_name != name` — identical result. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:remove@if node.type != \"script_element\":": "scan_template's script-element guard whose removal is masked downstream: only a script_element carries a raw_text child of JavaScript, so a non-script node has no raw_text and contributes no manifest keys; manifest extraction is idempotent over the appManifest object, so the union is unchanged. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:string->empty@wrapped = (\"(\" + raw + \")\").encode(\"utf-8\")@_hx_vals_keys": "_hx_vals_keys wraps the raw value in parens so `{...}` parses as an object expression, not a block. Emptying ONE paren (a single mutation) still surfaces the object: the remaining paren forces expression context, or tree-sitter's error recovery reinterprets `{...}` as an object when the stray close paren follows — verified `{a: 1})` and `({a: 1}` both yield the object node. An empty object gives empty keys whether found or not. Only removing BOTH parens (not a single mutation) would parse a block. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:string->empty@wrapped = (\"(\" + raw + \")\").encode(\"utf-8\")@_hx_vals_keys#1": "The close-paren of the same paren-wrap. Emptying it alone leaves the leading `(` forcing expression context, so `({a: 1}` still parses the object (verified); the extracted key set is unchanged and an empty object yields empty keys regardless. Only removing both parens (not a single mutation) would change parsing. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/boundary.py:remove@if not routes:": "boundary_diagnostics' fast-path: a file with no ROUTES skips the extraction and returns []. Removing the guard still runs the extractors and calls check_boundary with an empty route list, where chain_route is empty so every chain's route lookup misses and the result is the same []. The guard is a performance skip over the many non-app files, not a behavioural branch. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/boundary.py:sole-pass@return []": "The same fast-path's `return []` turned into pass: execution falls through to the extractors and check_boundary with an empty route list, which yields the same [] (no chain matches an empty route map). Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@js_class_references#1": "scan_template's script-element guard `continue` turned into pass: a non-script node then falls through to the raw_text scan, finds no raw_text child, and adds nothing — identical manifest keys. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@hf_references#1": "hf_references' attribute-type guard `continue` turned to pass: a non-attribute child then falls through to the attribute_name lookup, which yields None and is skipped by `name is None` — identical result, as in _attr. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:remove@if attribute.type != \"attribute\":@hf_references": "hf_references' attribute-type guard `if` removed: a non-attribute child then reaches the attribute_name lookup, yields None, and is skipped by `name is None` — identical result. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:string->empty@\"boundary (@boundary or @link(boundary=True)). See honest-state section 3.\",": "Diagnostic message text carries no behavioural signal: no conformance case or law asserts message wording — only the rule id, severity, and line are judged. Emptying the string leaves check_source's judged output identical across the full conformance battery. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:string->empty@f\"Persisted-state write '{name}' outside an I/O-boundary function. The single \"": "Diagnostic message text carries no behavioural signal: no conformance case or law asserts message wording — only the rule id, severity, and line are judged. Emptying the string leaves check_source's judged output identical across the full conformance battery. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:string->empty@\"mutator for persisted state is an honest-persist boundary write — move it to a \"": "Diagnostic message text carries no behavioural signal: no conformance case or law asserts message wording — only the rule id, severity, and line are judged. Emptying the string leaves check_source's judged output identical across the full conformance battery. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:remove@if node.type != \"call\":@check_hc_st001": "Type/shape guard whose removal is masked downstream: a non-call node yields a non-matching name from _qualified_call_name, so matches_watchlist rejects it and it is skipped identically. Verified byte-identical HC-ST001 output across fire / boundary / non-write / module-level / qualified / honest-persist-exempt inputs; equivalent.",
  "honest-check/src/honest_check/templates.py:remove@if attribute.type != \"attribute\":@hc_references": "Type/shape guard in hc_references whose removal is masked downstream: a non-attribute child of the open tag yields no attribute_name, so the name is None and the following hc- prefix guard skips it identically. A non-hc attribute name is caught (it would be appended). Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@hc_references#1": "Type/shape guard in hc_references whose removal is masked downstream: a non-attribute child of the open tag yields no attribute_name, so the name is None and the following hc- prefix guard skips it identically. A non-hc attribute name is caught (it would be appended). Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@scan_template": "manifest_keys' script-element guard continue whose neutralisation is masked downstream: a non-script node carries no raw_text child, so it contributes no manifest keys, and manifest extraction is idempotent over the union — the same equivalence as the already-declared script_element guard removal. Surfaced here only because inserting hc_references shifted the sole-continue numbering. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_st001": "Type/shape guard whose removal is masked downstream: a non-call node yields a non-matching name from _qualified_call_name, so matches_watchlist rejects it and it is skipped identically. Verified byte-identical HC-ST001 output across fire / boundary / non-write / module-level / qualified / honest-persist-exempt inputs; equivalent.",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p014#2": "check_hc_p014: neutralising the 'fewer than two types' skip lets a single-type recognizer proceed; it resolves to at most one slot and the 'if len(slots) < 2' guard skips it anyway. Equivalent (unchanged rules code; label index shifted after the HF rules were added).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p014": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc010#3": "check_hc010: neutralising the 'if not new_emits: continue' skip lets an empty new_emits proceed; phantom = new_emits - produced is then empty and the 'if not phantom' guard emits nothing. Equivalent (unchanged rules code; label index shifted after the HF rules were added).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc010#1": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@_produced_slot_keys": "Defensive loop skip in _produced_slot_keys on a non-pair dict child; falling through reads a None key whose value the 'if value is not None' guard already drops, so the key set is unchanged. Equivalent (unchanged rules code; label index shifted after the HF rules were added).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_hf001": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p005": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p004": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@_check_global_reads#1": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@_mutable_module_containers": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p016#1": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p016": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p007": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p011": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p001#1": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p001": "check_hc_p001's node.type guard continue (line 202): neutralising it lets a non-if node reach _if_chain_conditions, which returns no conditions for a non-if node, so targets is empty and the branch is skipped identically — the same masking as its already-declared guard-removal sibling. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p010#3": "Defensive loop skip on a node the body re-handles identically downstream (a None/empty result) or that cannot arise from honest source, so removing it does not change the loop's result. Equivalence verified empirically: applying the mutation leaves check_source output byte-identical across the full conformance battery (every rule case, message, severity, dedup, extractor source) plus enriched edge cases (multi-base classes, augmented/deleted subscripts, except*, multi-link chains, varied decorators).",
  "honest-check/src/honest_check/rules.py:remove@if not qualified:": "Guard whose removal is masked downstream: an unresolvable call name is the empty string, whose first character is not uppercase, so the following PascalCase test skips it identically. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/rules.py:0->-1@qualified = _qualified_call_name(values[0], source)": "A return statement carries exactly one named child — its expression — so values[0] and values[-1] are the same node. Equivalent.",
  "honest-check/src/honest_check/rules.py:1->2@name = qualified.rsplit(\".\", 1)[-1]@check_hc_p010": "The result takes the last segment, so maxsplit cannot change it: rsplit('.', n)[-1] is the final dotted segment for every n >= 1. Equivalent.",
  "honest-check/src/honest_check/rules.py:sole-pass@continue@check_hc_p010#2": "The continue of check_hc_p010's unresolvable-name guard. Neutralising it is masked downstream: an empty call name has no uppercase first character, so the PascalCase test skips it identically — the same equivalence as removing the guard itself. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/declgraph.py:sole-pass@continue@extract_composed_types": "Structural/type guard whose neutralisation is masked downstream: a non-call element yields None from child_by_field_name('function'), so the following `fn is None` guard skips it identically — the same equivalence already declared for this file's other node-type guards. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:remove@if node.type not in (\"lexical_declaration\", \"variable_declaration\"):": "js_module_bindings' declaration-type guard, and its continue. Removal is masked downstream: the declarator comprehension below already keeps only variable_declarator children, and no other top-level node carries one directly, so an unexpected node contributes no binding either way. Gate-confirmed equivalent.",
  "honest-check/src/honest_check/templates.py:sole-pass@continue@js_module_bindings": "js_module_bindings' declaration-type guard, and its continue. Removal is masked downstream: the declarator comprehension below already keeps only variable_declarator children, and no other top-level node carries one directly, so an unexpected node contributes no binding either way. Gate-confirmed equivalent."
}
