{
 "honest-persist/src/honest_persist/apply.py:1->2@ref_table, ref_column = definition[\"references\"].rsplit(\".\", 1)": "A column reference is 'table.column' with exactly one dot, so rsplit('.', 1) and rsplit('.', 2) return the same two parts.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#5": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#3": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#1": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#7": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:string->empty@\"postgresql\": frozenset(),": "Emptying/swapping the 'postgresql' key in _RECONSTRUCTION_OPS makes requires_reconstruction fall to the default empty frozenset, which is exactly what the postgres entry is \u2014 no operation requires reconstruction on postgres either way. Verified.",
 "honest-persist/src/honest_persist/apply.py:key->sibling@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:key->sibling@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#1": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:key->sibling@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#2": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:key->sibling@\"postgresql\": {\"uuid\": \"uuid\", \"timestamptz\": \"timestamptz\", \"boolean\": \"boolean\", \"jsonb\": \"jsonb\"},#3": "The postgresql type map is identity (uuid->uuid, etc.), so emptying or swapping a postgres KEY makes _sql_type fall to its default, which returns the abstract type unchanged \u2014 the same string the identity map would have produced. Verified across all types and dialects.",
 "honest-persist/src/honest_persist/apply.py:key->sibling@\"postgresql\": frozenset(),": "Emptying/swapping the 'postgresql' key in _RECONSTRUCTION_OPS makes requires_reconstruction fall to the default empty frozenset, which is exactly what the postgres entry is \u2014 no operation requires reconstruction on postgres either way. Verified.",
 "honest-persist/src/honest_persist/apply.py:remove@return None": "Removing the trailing `return None` at the end of _reconstruct's success path lets the function fall off its end and return None implicitly \u2014 identical.",
 "honest-persist/src/honest_persist/schema.py:remove@return None": "A `return None` reached on the success path; removing it lets the function fall off its end and return None implicitly \u2014 identical. Verified across schema-diff inputs.",
 "honest-persist/src/honest_persist/types.py:False->True@class Table(TypedDict, total=False):": "TypedDict total= is a static-only annotation with no runtime effect; total=True and total=False produce an identical class whose instances are plain dicts, and the schema dicts are assembled explicitly, so no constructible input distinguishes them.",
 "honest-persist/src/honest_persist/types.py:False->True@class Column(TypedDict, total=False):": "TypedDict total= is a static-only annotation with no runtime effect; total=True and total=False produce an identical class whose instances are plain dicts, and the schema dicts are assembled explicitly, so no constructible input distinguishes them.",
 "honest-persist/src/honest_persist/types.py:False->True@class Index(TypedDict, total=False):": "TypedDict total= is a static-only annotation with no runtime effect; total=True and total=False produce an identical class whose instances are plain dicts, and the schema dicts are assembled explicitly, so no constructible input distinguishes them.",
 "honest-persist/src/honest_persist/types.py:False->True@class Ambiguity(TypedDict, total=False):": "TypedDict total= is a static-only annotation with no runtime effect; total=True and total=False produce an identical class whose instances are plain dicts, and the schema dicts are assembled explicitly, so no constructible input distinguishes them.",
 "honest-persist/src/honest_persist/types.py:False->True@class Constraint(TypedDict, total=False):": "TypedDict total= is a static-only annotation with no runtime effect; total=True and total=False produce an identical class whose instances are plain dicts, and the schema dicts are assembled explicitly, so no constructible input distinguishes them.",
 "honest-persist/src/honest_persist/cutover.py:1->2@target = reference.rsplit(\".\", 1)[0]": "A foreign-key reference is 'table.column' with exactly one dot, so rsplit('.', 1)[0] and rsplit('.', 2)[0] return the same table part.",
 "honest-persist/src/honest_persist/loader.py:0->-1@return [arg for arg in get_args(annotation) if arg is not type(None)][0]": "_unwrap_optional runs only on an Optional annotation (X | None), whose non-None members are exactly one, so [0] and [-1] are the same element. Verified across Optional fields.",
 "honest-persist/src/honest_persist/loader.py:and->or@if default is not None and default is not PydanticUndefined:": "The _default is only consumed via default_sql, which returns None for both PydanticUndefined and None; the and->or only changes whether _default is stored as one of those, never the rendered column default. Verified the loaded columns are identical.",
 "honest-persist/src/honest_persist/loader.py:remove@if get_origin(annotation) is None:": "_is_optional's guard returns False for a non-generic annotation; removing it falls through to `type(None) in get_args(annotation)`, and get_args of a non-generic is (), so the result is False either way. Verified.",
 "honest-persist/src/honest_persist/loader.py:sole-pass@return False": "As above: the guard's `return False` -> pass falls through to `type(None) in get_args(annotation)`, which is False for a non-generic (empty get_args). Verified.",
 "honest-persist/src/honest_persist/loader.py:key->sibling@str: \"text\",": "Swapping the `str` key in _PY_TO_SQL drops str from the map, but _sql_type falls back to `.get(name, \"text\")` whose default is exactly \"text\" \u2014 str's mapping \u2014 so str still resolves to text. Verified.",
 "honest-persist/src/honest_persist/django_loader.py:string->empty@\"CharField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:string->empty@\"TextField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:string->empty@\"SlugField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:string->empty@\"EmailField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:string->empty@\"EmailField\": \"text\",#1": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:key->sibling@\"CharField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:key->sibling@\"TextField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:key->sibling@\"SlugField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged.",
 "honest-persist/src/honest_persist/django_loader.py:key->sibling@\"EmailField\": \"text\",": "A text-mapping entry: CharField/TextField/SlugField map to 'text' which is also the _dj_type fallback default, and EmailField reports get_internal_type()=='CharField' (a dead map entry), so emptying or swapping these keys/values leaves every field still resolving to 'text'. Verified the loaded columns are unchanged."
}