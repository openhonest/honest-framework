{
  "rules.js:<-><=@for (let i = 0; i < cssText.length; i++) {": "Loop bound < vs <=: <= runs one extra iteration at i === cssText.length, where cssText[i] is undefined and next is undefined. undefined equals none of the structural characters ('{', '}', quotes, '/', '*'), so the extra iteration takes no branch and changes no state. Behaviour-identical.",
  "rules.js:0->-1@for (let i = 0; i < cssText.length; i++) {": "Loop start 0 shifted to -1: the added iteration at i === -1 reads cssText[-1] === undefined, which matches no structural character and takes no branch. It is a no-op before the real walk begins. Behaviour-identical.",
  "rules.js:1->2@let braceOpen = -1;": "Initial braceOpen value: braceOpen is assigned the index of a depth-0 '{' before it is ever read, and a rule is only pushed (the sole reader, via cssText.slice(braceOpen + 1, i)) after that '{' set it. The initializer is never read. Behaviour-identical.",
  "rules.js:1->0@let braceOpen = -1;": "Initial braceOpen value: as above, braceOpen is always set at a depth-0 '{' before any slice reads it, so its initializer cannot affect output. Behaviour-identical.",
  "rules.js:string->empty@let mode = \"code\";": "The 'code' mode literal emptied to '': nothing ever compares mode === 'code'; 'code' is only the fall-through sentinel meaning 'not comment/single/double'. An initial '' fails the same three mode guards and takes the same character branches. Behaviour-identical.",
  "rules.js:string->empty@mode = \"code\";@splitRules": "A reset to 'code' emptied to '': as with the initializer, no comparison tests mode === 'code', so leaving a comment/string into '' behaves exactly as leaving it into 'code'. Behaviour-identical.",
  "rules.js:string->empty@mode = \"code\";@splitRules#1": "A second reset to 'code' emptied to '': behaves as above; 'code' is the none-of-the-above sentinel and '' is indistinguishable from it. Behaviour-identical.",
  "rules.js:string->empty@mode = \"code\";@splitRules#2": "A third reset to 'code' emptied to '': behaves as above; 'code' is the none-of-the-above sentinel and '' is indistinguishable from it. Behaviour-identical.",
  "style.js:0->1@const leader = (selector.match(/^(\\*|:root|[a-zA-Z][\\w-]*)/) || [\"\"])[0];": "Index [0] shifted to [1] on the leader match: the regex is a single capture group spanning the whole match, so on a match m[0] and m[1] are the identical string, and on no match the fallback [\"\"] gives \"\" at [0] and undefined at [1] - both fail GLOBAL_LEADERS.has and startsWith, so the selector is scoped either way. Behaviour-identical."
}
