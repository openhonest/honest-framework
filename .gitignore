.DS_Store
._*
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
*.egg-info/
dist/
build/
AGENTS.md
.agents.md
CLAUDE.md
python/honest_event_log.jsonl

# Abandoned prototype and local backup bundles — never released, never read (clean-slate rule).
python.botched/
framework backup/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Local beads tracker (dolt-backed working DB; not a release artefact)
.beads/

# Session-context aids, not release artefacts (see CLAUDE.md). Tracked by accident in the
# reflow commit through an over-broad `git add`; ignored here so it cannot recur.
.agents/
.claude/
.codex/

# Editor and generator backups.
*.bak

# macOS AppleDouble metadata. Four of these were tracked before today and carry nothing.
._*
